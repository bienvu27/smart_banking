import 'package:flutter/material.dart';

class QRCode_Page extends StatelessWidget {
  const QRCode_Page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('QR Code');
  }
}
